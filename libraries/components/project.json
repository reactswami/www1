{
   "name": "libraries-components",
   "$schema": "../../node_modules/nx/schemas/project-schema.json",
   "sourceRoot": "libraries/components/src",
   "projectType": "library",
   "targets": {
      "lint": {
         "executor": "nx:run-commands",
         "outputs": ["{options.outputFile}"],
         "options": {
            "command": "eslint . --quiet",
            "cwd": "libraries/components",
            "lintFilePatterns": ["libraries/components/**/*.{ts,tsx,js,jsx}"]
         }
      }
   },
   "tags": []
}
