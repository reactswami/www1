{
   "name": "libraries-ui",
   "$schema": "../../node_modules/nx/schemas/project-schema.json",
   "sourceRoot": "libraries/ui/src",
   "projectType": "library",
   "targets": {
      "lint": {
         "executor": "nx:run-commands",
         "outputs": ["{options.outputFile}"],
         "options": {
            "command": "eslint . --quiet",
            "cwd": "libraries/ui",
            "lintFilePatterns": ["libraries/ui/**/*.ts"]
         }
      }
   },
   "tags": []
}
