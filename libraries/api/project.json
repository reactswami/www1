{
   "name": "libraries-api",
   "$schema": "../../node_modules/nx/schemas/project-schema.json",
   "sourceRoot": "libraries/utils/src",
   "projectType": "library",
   "targets": {
      "lint": {
         "executor": "nx:run-commands",
         "outputs": ["{options.outputFile}"],
         "options": {
            "command": "eslint . --quiet",
            "cwd": "libraries/api",
            "lintFilePatterns": ["libraries/api/**/*.ts"]
         }
      }
   },
   "tags": []
}
