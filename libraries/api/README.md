# api

Libraries for all Statseeker server requests.
Grouped into the following categories:

## internal_api
Utilities for working with the internal_api CGI. This ise main way to access data.
Each dataype in the api will have its own folder under the entities subdir in this category.

## api-query-builder
Legacy library for building RESTful API queries.