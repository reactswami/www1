# Meraki poller

This package is the package containing the application to configure the Meraki custom poller.

## Mock server

Servers is mocked locally via `msw`. The handlers are located in `test/server/`.