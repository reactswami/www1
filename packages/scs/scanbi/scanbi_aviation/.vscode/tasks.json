{
   "tasks": [
      {
         "label": "Build ScanBI frontend package",
         "type": "shell",
         "group": "build",
         "presentation": {
            "echo": true,
            "reveal": "always",
            "focus": false,
            "panel": "shared",
            "showReuseMessage": true,
            "clear": true
         },
         "command": ". /home/${env:USER}/.bashrc;ssh ${env:USER}@${env:BUILD_SERVER} '. .profile;cd ${env:REMOTE_SRCDIR}/www; npx nx build scs-scanbi-aviation; cd ${env:REMOTE_SRCDIR}/www/dist/packages/scs/scanbi/scanbi-aviation;pwd;scp -r *  statseeker@${env:TEST_SERVER}:/usr/local/statseeker/scs/www/scanbi_aviation'",
         "dependsOn": ["Sync to build server"]
      }
   ]
}
