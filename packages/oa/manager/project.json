{
   "name": "oa-manager",
   "$schema": "../../../node_modules/nx/schemas/project-schema.json",
   "projectType": "application",
   "sourceRoot": "packages/oa/manager/src",
   "tags": ["oa"],
   "targets": {
      "build": {
         "executor": "@nx/vite:build",
         "options": {
            "outputPath": "dist/packages/oa-manager",
            "deleteOutputPath": true,
            "tsConfig": "packages/oa/manager/tsconfig.app.json"
         }
      },
      "lint": {
         "executor": "nx:run-commands",
         "outputs": ["{options.outputFile}"],
         "options": {
            "command": "eslint . --quiet",
            "cwd": "packages/oa/manager/src",
            "lintFilePatterns": ["packages/oa/manager/**/*.{ts,tsx,js,jsx}"]
         }
      }
   }
}
