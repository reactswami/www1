import { type ReactElement } from 'react';
import { TableRoute, RemoteTableRoute } from './TableRoute';

export const AppRoutes = (): ReactElement => <TableRoute />;
export const RemoveAppRoutes = (): ReactElement => <RemoteTableRoute />;
