
describe('<Table />', () => {
   it('is fine!', () => {
      expect(true).toBeTruthy();
   });
   // Tests have been moved up to the TableRoute component since it relies on context and API calls, it is easier to have higher integration tests
});
