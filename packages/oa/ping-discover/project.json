{
   "name": "oa-ping-discover",
   "$schema": "../../../node_modules/nx/schemas/project-schema.json",
   "version": "0.0.1",
   "projectType": "application",
   "sourceRoot": "packages/oa/ping-discover/src",
   "tags": ["oa"],
   "targets": {
      "build": {
         "executor": "@nx/vite:build",
         "options": {
            "outputPath": "dist/packages/oa-ping-discover",
            "deleteOutputPath": true,
            "tsConfig": "packages/oa/ping-discover/tsconfig.app.json"
         }
      },
      "lint": {
         "executor": "nx:run-commands",
         "outputs": ["{options.outputFile}"],
         "options": {
            "command": "eslint . --quiet",
            "cwd": "packages/oa/ping-discover",
            "lintFilePatterns": ["packages/oa/ping-discover/**/*.{ts,tsx,js,jsx}"]
         }
      }
   }
}
