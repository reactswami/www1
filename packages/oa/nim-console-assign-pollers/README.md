# nim-console-assign-pollers

This project contains a simple page that renders the modal to assign pollers.
It is used from the nim console, device health viewer.

Historically, when clicking on the ping cell it would turn on/off the ping polling.
Now, it opens this package which renders a modal that allows assigning pollers to the device.

This is part of the OA feature.
